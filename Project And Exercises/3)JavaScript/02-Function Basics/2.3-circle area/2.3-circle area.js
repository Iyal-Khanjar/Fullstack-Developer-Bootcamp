function CircleArea(radius)
{
        let x=Math.PI * Math.pow(radius,2)
        let y= x.toFixed(2);
        console.log('Area= '+ x);
        console.log('rounded to two decimal places= '+ y)
        
    };
CircleArea(3)

 